sources:
  - ansible.eda.webhook:
      host: 0.0.0.0
      port: 5000
rules:
  - name: webhook received
    condition: event.payload is defined
    action:
      run_playbook:
        name: ../playbooks/simple_debug.yml
